import type { Metadata } from 'next';
import Link from 'next/link';
import { commpolarCategories, commpolarProducts } from '@/lib/data';
export const metadata:Metadata={title:'Commpolar Authorized Partner India | Optical Test Equipment',description:'RF Equipment is an authorized Commpolar partner for India, supplying optical, WDM, PLC, BOB, GPON/XGS-PON/FTTR and transceiver test equipment.',alternates:{canonical:'/commpolar'}};
export default function Page(){return <><section className="page-hero"><div className="container"><p className="eyebrow">Authorized Partner · India</p><h1>Commpolar optical & telecom production test solutions.</h1><p className="lead">RF Equipment supports Commpolar product selection, application engineering, quotations, integration and local technical coordination in India.</p></div></section><section className="section"><div className="container"><p className="lead">Every model below links to a dedicated product page. Images marked representative show the same Commpolar product family when an exact model photo is not available in the repository.</p>{commpolarCategories.map(category=><section className="catalogue-section" key={category}><h2 className="category-title">{category}</h2><div className="product-gallery">{commpolarProducts.filter(p=>p.category===category).map(p=><article className="product-card" key={p.slug}><div className="product-photo"><img src={p.image} alt={`${p.model} ${p.name}`}/></div><div className="product-body"><span className="tag">Commpolar</span><h3>{p.model}</h3><p>{p.name}</p>{p.representativeImage&&<small className="image-note">Representative family image</small>}<Link className="card-link" href={`/commpolar/${p.slug}`}>View product →</Link></div></article>)}</div></section>)}</div></section></>}
