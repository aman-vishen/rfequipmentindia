import type { Metadata } from 'next';
import Link from 'next/link';
import { notFound } from 'next/navigation';
import { commpolarProducts, site } from '@/lib/data';
export function generateStaticParams(){return commpolarProducts.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=commpolarProducts.find(x=>x.slug===slug);if(!p)return{};return{title:`${p.model} ${p.name} India`,description:`${p.model} ${p.name} from Commpolar in India through RF Equipment. ${p.summary}`,alternates:{canonical:`/commpolar/${p.slug}`},openGraph:{images:[p.image]}}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=commpolarProducts.find(x=>x.slug===slug);if(!p)notFound();const jsonLd={'@context':'https://schema.org','@type':'Product',name:`${p.model} ${p.name}`,image:`${site.url}${p.image}`,description:p.summary,brand:{'@type':'Brand',name:'Commpolar'},category:p.category};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><section className="page-hero"><div className="container"><div className="breadcrumb"><Link href="/">Home</Link><span>›</span><Link href="/commpolar">Commpolar</Link><span>›</span><span>{p.model}</span></div><p className="eyebrow">Commpolar · Authorized Partner India</p><h1>{p.model}</h1><p className="lead">{p.name}</p></div></section><section className="section"><div className="container split product-detail"><figure className="product-detail-image"><img src={p.image} alt={`${p.model} ${p.name}`}/>{p.representativeImage&&<figcaption>Representative Commpolar product-family image; exact appearance may vary by model revision.</figcaption>}</figure><div><span className="tag">{p.category}</span><h2>{p.name}</h2><p className="lead">{p.summary}</p><div className="detail-list"><div><b>Brand</b><span>Commpolar</span></div><div><b>Model</b><span>{p.model}</span></div><div><b>India Partner</b><span>RF Equipment</span></div><div><b>Availability</b><span>Request quotation / configuration</span></div></div><div className="actions"><Link className="btn btn-primary" href={`/contact?product=${encodeURIComponent(p.model)}`}>Request Quote</Link><Link className="btn btn-outline" href="/commpolar">Back to Catalogue</Link></div></div></div></section></>}
