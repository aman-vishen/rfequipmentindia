import Link from 'next/link';
import { shieldBoxes } from '@/lib/data';
export default function Page(){return <><section className="page-hero"><div className="container"><p className="eyebrow">RF Shield Boxes</p><h1>Manual and pneumatic RF isolation enclosures.</h1><p className="lead">Shielded enclosures for wireless R&D and telecom production test.</p></div></section><section className="section"><div className="container"><div className="product-gallery">{shieldBoxes.map((p)=><article className="product-card" key={p.slug}><div className="product-photo"><img src={p.image} alt={p.model}/></div><div className="product-body"><span className="tag">{p.type}</span><h3>{p.model}</h3><p>{p.summary}</p><Link className="card-link" href={`/shield-boxes/${p.slug}`}>View product</Link></div></article>)}</div></div></section></>}
