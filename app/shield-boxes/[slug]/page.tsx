import type { Metadata } from 'next';
import Link from 'next/link';
import { notFound } from 'next/navigation';
import { shieldBoxes } from '@/lib/data';

export function generateStaticParams(){return shieldBoxes.map(p=>({slug:p.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=shieldBoxes.find(x=>x.slug===slug);return p?{title:`${p.model} RF Shield Box India`,description:p.summary,alternates:{canonical:`/shield-boxes/${p.slug}`}}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=shieldBoxes.find(x=>x.slug===slug);if(!p)notFound();return <><section className="page-hero"><div className="container"><div className="breadcrumb"><Link href="/shield-boxes">Shield Boxes</Link><span>›</span><span>{p.model}</span></div><p className="eyebrow">{p.type} RF Shield Box</p><h1>{p.model}</h1><p className="lead">{p.summary}</p></div></section><section className="section"><div className="container split product-detail"><figure className="product-detail-image"><img src={p.image} alt={p.model}/>{p.representativeImage&&<figcaption>Representative catalogue image. Request exact model imagery and mechanical drawing with quotation.</figcaption>}</figure><div><h2>Configure the enclosure around your DUT.</h2><ul className="clean-list dark-text"><li>RF / SMA / N-Type feedthrough options</li><li>Ethernet, USB, DC and AC interfaces</li><li>Internal antenna or coupler integration</li><li>DUT fixture and pogo-pin integration</li><li>Pneumatic and PLC control options</li></ul><div className="actions"><Link className="btn btn-primary" href={`/contact?product=${encodeURIComponent(p.model)}`}>Request Specifications</Link></div></div></div></section></>}
