import type { Metadata } from 'next';
import { Suspense } from 'react';
import { ContactForm } from '@/components/ContactForm';
import { site } from '@/lib/data';
export const metadata:Metadata={title:'Contact & Request a Quotation',description:'Contact RF Equipment India for RF, Wi-Fi, optical, GPON, shield box and turnkey telecom manufacturing test solutions.',alternates:{canonical:'/contact'}};
export default function Page(){return <><section className="page-hero"><div className="container"><p className="eyebrow">Contact & RFQ</p><h1>Tell us what you need to test.</h1><p className="lead">Share your DUT, chipset, test items and production target for an equipment recommendation or turnkey proposal.</p></div></section><section className="section"><div className="container rfq-layout"><aside className="contact-meta"><h2>RF Equipment</h2><p>Email<br/><a href={`mailto:${site.email}`}>{site.email}</a></p><p>Phone / WhatsApp<br/><a href={`tel:${site.phone}`}>{site.phoneDisplay}</a></p></aside><div><h2>Request a quotation</h2><p className="lead">Complete the form with your DUT, required measurements and target UPH.</p><Suspense fallback={<div className="rfq-form">Loading form…</div>}><ContactForm/></Suspense></div></div></section></>}
